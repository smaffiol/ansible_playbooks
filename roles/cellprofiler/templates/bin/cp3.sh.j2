#!/bin/bash

# Run cellprofiler 3 docker image
sudo docker run --net=host --env="DISPLAY" --volume="$HOME/.Xauthority:/root/.Xauthority:rw" -v {{ data_path }}:{{ data_path }} -v {{ plugins_path }}:{{ plugins_path }} {{ cp3_docker }} -p $@ -c -r -o {{ data_path }} --plugins-directory={{ plugins_path }}

